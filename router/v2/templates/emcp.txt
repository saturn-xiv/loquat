#!/bin/bash

set -e

ip route replace default scope global {{ range $dev, $eth := . }} nexthop via $(ip route show dev {{ $dev }} | awk '/default/ {print $3}') dev {{ $dev }} weight {{ $eth.weight }} {{ end }}

echo 'done.'
exit 0
